# -*- coding: utf-8 -*-
"""
检索评测语料 v3 —— 高区分度 + 长文本版

针对 v2 的问题修正：每节语料加长到 450~650 字，保证子块（200 字符/重叠 50）
能真正从父块中切出 2~4 个，让"小子块精确召回 + 父块回取"的父子分块价值被真实测出。

设计：29 个父块，主题聚类制造相似干扰（缓存穿透/击穿/雪崩、令牌桶/漏桶等）。
"""

SECTIONS = [
    # ============ 数据库索引（4） ============
    {"id": "D01", "title": "B+树索引结构",
     "text": (
         "B+树是多路平衡搜索树，是关系型数据库最常用的索引结构。"
         "它的内部节点只保存键值和指向子节点的指针，不存储具体数据记录，"
         "所有数据记录都集中在叶子节点，叶子节点之间通过双向链表串联。"
         "这种设计带来两个好处：一是单个内部节点能容纳更多键，树的高度更低，"
         "从根节点到叶子节点的磁盘 IO 次数更少；二是范围查询可以顺着叶子链表顺序扫描。"
         "等值查询时从根节点逐层下探到叶子节点，范围查询在找到起点后沿链表前进。"
         "与 B 树相比，B+树的内部节点不存数据，同样大小的页能索引更多记录，"
         "因此查询性能更稳定，且天然支持范围扫描和排序。"
         "索引并非越多越好，写操作需要同步维护树结构，索引越多插入更新越慢，"
         "还占用额外存储空间，因此要在查询加速和写入开销之间权衡。"
     )},
    {"id": "D02", "title": "哈希索引",
     "text": (
         "哈希索引把键通过哈希函数映射到桶，等值查询可以在常数时间内定位目标，"
         "速度比 B+树更快。但它不保存键的顺序，因此无法回答范围查询，"
         "也不能用于排序和前缀匹配。"
         "当查询恰好是精确等值时，哈希索引表现最好，"
         "但一旦查询涉及区间、排序或模糊匹配，哈希索引就无能为力。"
         "哈希索引还面临哈希冲突问题，多个键可能映射到同一个桶，"
         "需要链地址法或开放寻址法解决冲突，冲突严重时会退化为线性查找。"
         "数据库通常把哈希索引用于内存表或精确查找场景，"
         "通用查询场景下仍以 B+树为主，因为大多数业务查询都包含范围或排序条件。"
     )},
    {"id": "D03", "title": "覆盖索引与回表",
     "text": (
         "回表是指数据库先通过二级索引找到主键，"
         "再根据主键到主键索引中查找完整数据行的过程。"
         "回表会带来额外的随机 IO，当查询需要回表的行数很多时，性能会明显下降。"
         "覆盖索引是一种查询优化手段，它让索引本身就包含查询需要的全部列，"
         "这样数据库无需再回到主表取数据，一次索引扫描就能返回结果，避免了回表。"
         "覆盖索引通过把常用查询列组合进索引实现，"
         "代价是索引体积变大、维护成本上升，写入时也要更新更多索引。"
         "是否使用覆盖索引，取决于查询的列集合能否被某个索引完整覆盖。"
     )},
    {"id": "D04", "title": "联合索引与最左前缀",
     "text": (
         "联合索引把多个列组合成一个索引，遵循最左前缀原则，"
         "查询条件只有命中联合索引的最左侧列，索引才会被充分利用。"
         "例如在 (a, b, c) 三列上建联合索引，"
         "查询条件包含 a 时能用索引，只包含 b 或 c 时索引基本失效。"
         "设计联合索引时，要把区分度高、经常作为过滤条件的列放在最前面。"
         "违反最左前缀会导致索引失效或部分失效，查询退化为全表扫描。"
         "联合索引还能带来覆盖索引的好处，如果查询列都在联合索引内，"
         "就可以避免回表。因此联合索引的列顺序设计直接影响查询性能。"
     )},

    # ============ 缓存（4）—— 高混淆区 ============
    {"id": "C01", "title": "缓存穿透",
     "text": (
         "缓存穿透指查询一个根本不存在的数据，缓存中自然也没有，"
         "导致请求每次都穿透缓存直接落到数据库上。"
         "攻击者可以利用大量不存在的 key 持续打库，把数据库拖垮。"
         "缓存穿透的典型特征是请求的数据在数据库里根本不存在，"
         "因此无论缓存是否命中，最终都会查询数据库。"
         "解决缓存穿透的常见手段有两种：一是缓存空值，"
         "当查不到数据时也把空结果写入缓存并设置较短的过期时间，"
         "这样后续相同请求直接命中空缓存，不再打库。"
         "二是使用布隆过滤器，在请求进入缓存前先判断 key 是否可能存在。"
         "布隆过滤器通过多个哈希函数把元素映射到位数组上，"
         "查询时只要有一个位为 0 就说明元素一定不存在，"
         "如果所有位都为 1 则说明可能存在，需要进一步查库确认。"
         "布隆过滤器空间占用极小，但存在误判率，只能判断一定不存在或可能存在。"
     )},
    {"id": "C02", "title": "缓存击穿",
     "text": (
         "缓存击穿指某个热点 key 在过期的瞬间，大量并发请求同时打到数据库，"
         "因为这一刻缓存已经失效，所有请求都绕过缓存直接查询数据库。"
         "缓存击穿针对的是单个热点 key，这个 key 通常承载着极高的访问量，"
         "一旦过期，瞬间的并发流量会形成击穿效应。"
         "常见的解决方法是加互斥锁，让第一个请求去查库并回写缓存，"
         "其他请求等待锁释放后直接读缓存；也可以采用逻辑过期，"
         "在缓存值里额外记录一个逻辑过期时间，物理上不过期，"
         "读到的线程发现逻辑过期后异步去刷新缓存。"
         "击穿与穿透的本质区别在于：击穿的 key 在数据库里是存在的，"
         "只是缓存刚好失效；穿透的 key 在数据库里根本不存在。"
     )},
    {"id": "C03", "title": "缓存雪崩",
     "text": (
         "缓存雪崩指大量缓存在同一时间集中过期，或缓存服务整体宕机，"
         "导致请求瞬间全部压向数据库。"
         "雪崩针对的是大面积 key，与单个热点 key 的击穿不同。"
         "当缓存层整体不可用时，所有读请求都会落到数据库，"
         "数据库在瞬间的高并发下很容易被打垮，进而引发级联故障。"
         "解决雪崩的办法是给过期时间加上随机抖动，避免大量 key 同时过期；"
         "或者设置多级缓存，本地缓存加分布式缓存，降低单点风险；"
         "还可以对数据库做限流和熔断，保护底层存储不被冲垮。"
         "击穿是单个热点 key 的问题，雪崩是大面积失效的问题，"
         "两者本质都是缓存失效后流量直接击穿到存储层，但影响范围不同。"
     )},
    {"id": "C04", "title": "缓存淘汰策略LRU与LFU",
     "text": (
         "缓存容量有限，当缓存写满时需要淘汰部分数据腾出空间，"
         "常见的淘汰策略有 LRU 和 LFU。"
         "LRU 淘汰最近最少使用的数据，基于访问时间，"
         "适合有明显时间局部性的场景，即最近被访问的数据大概率再次被访问。"
         "LRU 的经典实现是哈希表加双向链表，访问时把节点移到链表头部，"
         "淘汰时删除链表尾部节点。"
         "LFU 淘汰使用频率最低的数据，基于访问次数，"
         "适合热点稳定但偶发访问多的场景，能防止低频数据长期占据缓存。"
         "LFU 需要额外维护频率计数，实现比 LRU 复杂，"
         "且对新数据不友好，因为新数据频率低容易被淘汰，"
         "实际使用中常结合时间衰减来缓解这个问题。"
     )},

    # ============ 消息队列（3） ============
    {"id": "M01", "title": "消息队列削峰解耦",
     "text": (
         "消息队列是分布式系统中用于异步通信的中间件，"
         "核心价值在于解耦、异步和削峰填谷。"
         "解耦是指生产者和消费者不直接调用，而是通过队列中转，"
         "双方可以独立演进，只要约定好消息格式即可。"
         "异步是指生产者把消息写入队列后立即返回，"
         "不需要等待消费者处理完成，从而降低接口响应时间。"
         "削峰填谷则是把瞬间的高并发请求暂存在队列里，"
         "让消费者按照自己的能力匀速处理，避免后端服务被突发流量冲垮。"
         "典型场景是秒杀活动，前端把请求先写入消息队列，"
         "后端按固定速率消费，库存扣减等操作在消费者侧完成。"
         "削峰的本质是用队列的积压能力换取处理能力的平滑，"
         "代价是引入了延迟和消息堆积的风险，"
         "需要监控队列长度并在堆积时及时告警和扩容消费者。"
     )},
    {"id": "M02", "title": "消息幂等与重复消费",
     "text": (
         "消息重复是分布式系统中无法完全避免的，因为网络重传和重试都会导致重复投递，"
         "因此消费者必须实现幂等，即同一条消息处理多次的结果与处理一次相同。"
         "如果消费者不具备幂等性，重复消息会导致数据被重复写入或操作被重复执行，"
         "例如重复扣款、重复下单等严重问题。"
         "幂等的常用手段是给每条消息分配全局唯一的消息 ID，"
         "消费端用数据库或 Redis 记录已处理的消息 ID，重复消息直接跳过；"
         "也可以利用数据库唯一约束，让重复写入天然失败。"
         "实现幂等的前提是消息 ID 全局唯一且持久化，"
         "同时要考虑消息处理失败后的重试与幂等记录的一致性。"
     )},
    {"id": "M03", "title": "死信队列与消息可靠性",
     "text": (
         "消息可靠性需要处理重复、丢失和积压三类问题。"
         "消息丢失可能发生在生产端、传输端或消费端，"
         "生产端通过发送确认机制保证消息成功写入队列，"
         "消费端通过手动确认 ack，只有处理成功才确认，失败则重试。"
         "死信队列用于接收无法被正常消费的消息，"
         "当一条消息重试次数超过阈值仍然失败时，就被投递到死信队列，"
         "由人工或定时任务排查原因，避免阻塞后续消息。"
         "消息积压则发生在消费速度跟不上生产速度时，"
         "需要监控队列长度，及时扩容消费者或优化消费逻辑。"
         "可靠性设计的核心思想是宁可重复也不丢失，"
         "因为重复可以通过幂等解决，而丢失则无法补救。"
     )},

    # ============ 分布式一致性（4）—— 高混淆区 ============
    {"id": "T01", "title": "CAP定理",
     "text": (
         "CAP 定理指出，分布式系统在分区容错的前提下，"
         "一致性和可用性无法同时满足，只能二选一。"
         "一致性指所有节点在同一时刻看到相同的数据，"
         "可用性指每个请求都能在合理时间内得到响应，"
         "分区容错指系统在网络分区故障时仍能继续运行。"
         "由于网络分区在分布式环境中不可避免，"
         "因此系统必须在 CP 和 AP 之间权衡。"
         "CP 系统在分区时牺牲可用性保证一致性，典型如 ZooKeeper；"
         "AP 系统在分区时牺牲一致性保证可用性，典型如多数互联网服务。"
         "理解 CAP 的关键是认识到分区是常态，"
         "所谓选择其实是在一致性和可用性之间做取舍。"
     )},
    {"id": "T02", "title": "BASE理论与最终一致",
     "text": (
         "BASE 理论是对强一致性的放松，主张基本可用、软状态和最终一致。"
         "基本可用指系统在故障时仍能提供核心功能，允许性能下降；"
         "软状态指允许系统存在中间状态，不同节点的数据副本可以短暂不一致；"
         "最终一致指系统经过一段时间后，所有副本最终达到一致。"
         "最终一致相比强一致能获得更好的性能和可用性，"
         "代价是用户可能读到旧数据，需要在业务层面容忍短暂的不一致。"
         "BASE 理论是 AP 系统设计的思想基础，"
         "它承认分布式环境下强一致的成本过高，"
         "转而追求一种可接受的、最终收敛的一致性。"
     )},
    {"id": "T03", "title": "两阶段提交2PC",
     "text": (
         "两阶段提交 2PC 是分布式事务的经典方案，"
         "目标是保证跨多个节点的操作要么全部成功要么全部回滚。"
         "第一阶段准备，协调者向所有参与者发送准备请求，"
         "参与者执行本地事务并锁定资源，返回是否可以提交；"
         "第二阶段提交，协调者根据所有参与者的反馈，"
         "统一发送提交或回滚指令。"
         "2PC 的问题是同步阻塞，参与者在等待协调者指令时资源被锁定；"
         "协调者单点故障，协调者宕机后事务悬挂；"
         "参与者超时后的不确定性，部分提交部分回滚会导致不一致。"
         "因此 2PC 适合低延迟、强一致要求的场景，"
         "不适合高并发、跨地域的分布式事务。"
     )},
    {"id": "T04", "title": "TCC与补偿事务",
     "text": (
         "TCC 事务把操作拆成 Try、Confirm、Cancel 三个阶段。"
         "Try 阶段预留资源并检查业务条件，"
         "Confirm 阶段确认提交并真正执行业务，"
         "Cancel 阶段释放预留的资源。"
         "TCC 相比 2PC 的优点是资源不长时间锁定，可用性更好。"
         "对于无法强一致的场景，更常用的是最终一致加补偿，"
         "即先把主流程做完，失败的部分通过补偿操作逆向撤销。"
         "补偿操作本身也可能失败，因此需要引入重试、"
         "状态机标记和定期对账来保证最终可恢复。"
         "对账是指后台定期比对各方数据，发现不一致后自动修复。"
     )},

    # ============ 负载均衡与分片（3） ============
    {"id": "L01", "title": "负载均衡算法",
     "text": (
         "负载均衡把请求分发到多个后端节点，提高系统的吞吐和可用性，"
         "常用算法有轮询、加权轮询和最少连接。"
         "轮询按顺序依次把请求分配给每个节点，实现简单且均匀；"
         "加权轮询根据节点的处理能力分配不同权重，能力强的节点多承担请求；"
         "最少连接把请求发给当前连接数最少的节点，适合请求处理耗时差异大的场景。"
         "负载均衡还分为四层和七层，四层基于传输层信息分发，"
         "七层基于应用层内容分发，可以做更精细的路由。"
         "健康检查是负载均衡的重要组成，"
         "通过定期探测剔除故障节点，保证请求不会被分发到不可用的节点。"
     )},
    {"id": "L02", "title": "一致性哈希",
     "text": (
         "一致性哈希解决的是缓存或分片在节点增减时的数据迁移问题。"
         "它把节点和 key 都映射到一个环形哈希空间上，"
         "key 顺时针找到的第一个节点就是它所属的节点。"
         "当节点增加或减少时，只有相邻的一小段 key 需要迁移，"
         "避免了普通取模哈希导致的大面积数据重分配。"
         "普通取模哈希在节点数变化时，几乎所有 key 的映射都会改变，"
         "导致缓存大面积失效，引发雪崩。"
         "一致性哈希为了缓解节点在环上分布不均带来的倾斜问题，"
         "引入虚拟节点，每个真实节点对应多个虚拟节点，"
         "让 key 在环上的分布更加均匀。"
     )},
    {"id": "L03", "title": "哈希槽与Redis集群",
     "text": (
         "Redis 集群的哈希槽把整个哈希空间划分为固定数量的槽位，"
         "一般为 16384 个，槽位显式分配给不同节点。"
         "key 通过 CRC16 算法计算出归属的槽位，再由槽位定位到对应的节点。"
         "与一致性哈希不同，哈希槽是固定数量、显式分配的，"
         "集群通过槽位的转移来实现节点的扩容和缩容。"
         "Redis 集群的每个节点负责一部分槽位，"
         "客户端根据槽位映射表把请求路由到正确的节点。"
         "哈希槽方案的好处是槽位数量固定，迁移粒度可控，"
         "可以精确地把某个槽位从旧节点迁移到新节点。"
     )},

    # ============ 限流与保护（4）—— 高混淆区 ============
    {"id": "R01", "title": "计数器与滑动窗口限流",
     "text": (
         "限流用于控制单位时间内的请求量，保护系统不被突发流量打垮。"
         "计数器算法是最简单的，统计一个时间窗口内的请求数，超过阈值就拒绝。"
         "计数器算法的问题是窗口切换时存在临界问题，"
         "在窗口边界的瞬间可能放过接近两倍的流量。"
         "滑动窗口把时间切得更细，统计的是最近一段时间内的请求数，"
         "而不是固定窗口的整块统计，因此比固定窗口更平滑。"
         "滑动窗口可以用环形数组或 Redis 的 ZSet 实现，"
         "记录每个请求的时间戳，查询时统计最近窗口内的请求数。"
         "窗口越小限流越精确，但内存和时间开销也越大。"
     )},
    {"id": "R02", "title": "令牌桶算法",
     "text": (
         "令牌桶算法以固定速率向桶中放入令牌，"
         "请求到来时需要取走一个令牌才能被处理，"
         "桶满后多余的令牌被丢弃，桶空时请求被拒绝或等待。"
         "令牌桶允许一定程度的突发流量，因为桶里可以积攒令牌，"
         "当一段时间内没有请求时，令牌会累积到桶上限，"
         "突发流量到来时可以一次性消耗积攒的令牌。"
         "令牌桶适合需要容忍瞬时突发的场景，"
         "它限制的是长期的平均速率，而不是瞬时的峰值。"
         "令牌桶的两个关键参数是令牌生成速率和桶容量，"
         "生成速率决定长期平均限流值，桶容量决定允许的突发大小。"
     )},
    {"id": "R03", "title": "漏桶算法",
     "text": (
         "漏桶算法让请求像水一样匀速流出，出水速率固定，"
         "不管进来的流量多大，出去的速率都保持恒定。"
         "漏桶能严格平滑流量，把突发流量整形为匀速流，"
         "但无法应对突发，因为超出速率的请求会被排队或丢弃。"
         "漏桶和令牌桶的核心区别在于，令牌桶允许突发，漏桶强制匀速。"
         "令牌桶关注的是放行速率，桶里积攒的令牌可以支持突发；"
         "漏桶关注的是输出速率，无论输入如何，输出始终恒定。"
         "选择哪个算法取决于业务是否需要容忍突发，"
         "需要突发的场景用令牌桶，需要严格匀速的场景用漏桶。"
     )},
    {"id": "R04", "title": "熔断器与降级",
     "text": (
         "熔断器用于在依赖服务故障时快速失败，避免请求堆积和级联故障。"
         "熔断器通常有三种状态：闭合、打开和半开。"
         "闭合状态正常放行请求，并统计失败率，"
         "当失败率超过阈值时切换到打开状态。"
         "打开状态直接拒绝所有请求并快速返回错误，"
         "不再调用下游，给故障服务恢复的时间。"
         "经过一段冷却时间后进入半开状态，"
         "半开状态允许少量请求试探下游是否恢复，"
         "成功则回到闭合，失败则继续打开。"
         "降级是在依赖不可用时提供兜底方案，"
         "比如返回缓存数据、默认值或友好的错误提示，保证核心功能可用。"
         "熔断和降级通常配合超时和重试一起使用。"
     )},

    # ============ 向量检索（3） ============
    {"id": "V01", "title": "向量embedding与相似度",
     "text": (
         "向量检索把文本、图片等对象通过 embedding 模型编码成稠密向量，"
         "语义相近的内容在向量空间中距离更近。"
         "embedding 模型把一句话映射到一个固定维度的向量，"
         "例如 1024 维，向量的每个分量都是浮点数。"
         "向量之间的相似度可以用余弦相似度计算，"
         "余弦相似度衡量的是两个向量方向的夹角，取值在 -1 到 1 之间，"
         "值越大表示方向越一致，文本越相似。"
         "除余弦外还有欧氏距离 L2 和内积两种度量，"
         "文本向量通常先归一化再计算，此时余弦相似度和内积等价。"
         "向量检索的核心价值是语义匹配，即使查询词和文档用词不同，"
         "只要语义相近也能被检索到，这是传统关键词匹配做不到的。"
         "代价是 embedding 是模型计算的结果，"
         "对专有名词、编号等精确匹配场景可能不如关键词检索可靠。"
     )},
    {"id": "V02", "title": "HNSW近似最近邻",
     "text": (
         "精确最近邻搜索在数据量大时计算代价过高，"
         "因此向量数据库普遍采用近似最近邻 ANN 算法，用少量精度损失换取速度。"
         "HNSW 是分层可导航小世界图，是当前最主流的 ANN 算法之一。"
         "它构建多层图，高层节点稀疏、负责长距离跳转，"
         "低层节点密集、负责精细搜索，"
         "查询时从顶层开始逐层下降，在每一层做贪心搜索。"
         "HNSW 有三个关键参数，M 控制每个节点的最大连接数，"
         "efConstruction 控制建图时的搜索宽度，ef 控制查询时的搜索宽度，"
         "参数越大召回率越高，但时间和内存开销也越大。"
         "HNSW 的查询复杂度近似对数级别，在高召回率要求下性能优秀，"
         "但内存占用相对较高，因为需要存储图结构。"
     )},
    {"id": "V03", "title": "IVF与PQ量化",
     "text": (
         "IVF 倒排索引先把向量聚类成多个中心，"
         "查询时只搜索距离最近的几个簇，减少搜索范围，"
         "适合大规模数据的加速检索。"
         "PQ 乘积量化把向量分段量化压缩，降低内存占用，"
         "用近似表示换取更小的存储和更快的计算。"
         "IVF 和 PQ 适合超大规模数据，与 HNSW 相比内存占用更低，"
         "但召回率通常略低，需要调节簇数等参数。"
         "实际系统中常把 IVF 和 PQ 结合使用，"
         "先用 IVF 缩小搜索范围，再用 PQ 压缩向量，"
         "在召回率和内存之间取得平衡。"
         "选择 ANN 算法要综合考虑数据规模、内存预算和召回率要求。"
     )},

    # ============ RAG 检索（4） ============
    {"id": "G01", "title": "RAG架构与流程",
     "text": (
         "RAG 即检索增强生成，核心思想是在大模型回答问题之前，"
         "先从外部知识库检索相关文档，把检索结果拼进提示词，"
         "让模型基于检索到的内容生成答案。"
         "RAG 解决大模型的两个痛点：一是幻觉，"
         "模型在缺乏知识时容易编造，RAG 用真实文档约束回答；"
         "二是知识时效性，外部知识库可以随时更新，"
         "而不需要重新训练模型。"
         "RAG 的流程通常分为索引、召回和生成三个阶段，"
         "索引阶段把文档切块并向量化存入向量库，"
         "召回阶段根据查询检索最相关的块，"
         "生成阶段把召回结果拼进提示词交给大模型。"
         "召回的质量直接决定最终答案质量，"
         "如果召回的相关文档是错的，再好的模型也会给出错误答案，"
         "这就是常说的垃圾进垃圾出。"
     )},
    {"id": "G02", "title": "重排序与交叉编码器",
     "text": (
         "重排序 rerank 是 RAG 流程中的精排阶段。"
         "召回阶段通常用双塔模型，把查询和文档分别编码成向量后计算相似度，"
         "这种方式速度快，可以处理海量候选，但精度有限，"
         "因为查询和文档在编码时没有交互。"
         "重排序使用交叉编码器，把查询和文档拼接在一起输入模型，"
         "让模型同时对两者进行注意力计算，充分建模交互关系，"
         "因此相关性判断更准确。"
         "交叉编码器的代价是计算慢，只能对少量候选做精排，"
         "所以典型流程是先用双塔召回 top-N 个候选，"
         "再用交叉编码器对 top-N 精排，取最终的 top-k。"
         "重排序能显著提升最终答案的相关性，"
         "但增加了延迟和计算成本，需要在效果和性能之间权衡。"
     )},
    {"id": "G03", "title": "父子分块",
     "text": (
         "文档分块是 RAG 的基础，分块大小是精度和上下文的矛盾："
         "块太小则召回更精确但信息碎片化，"
         "块太大则上下文完整但向量被稀释、召回不准。"
         "父子分块把两者解耦，用小子块做向量检索保证命中率，"
         "命中后通过父子关系回取大父块喂给大模型保证上下文完整。"
         "具体实现上，把文档切成较大的父块，每个父块再切成较小的子块，"
         "子块入库做向量检索，父块存关系库用于回取。"
         "父子分块同时获得了小子块的高召回精度和大父块的完整上下文，"
         "代价是多一次父块回查和额外的父块存储。"
     )},
    {"id": "G04", "title": "混合检索与RRF",
     "text": (
         "混合检索融合稠密向量检索和稀疏关键词检索。"
         "稠密检索擅长语义相近但用词不同的情况，"
         "稀疏检索如 BM25 擅长精确词、编号和专有名词，两者互补。"
         "融合常用 RRF 倒数排名融合，"
         "它只关心两路结果各自的排名而不关心分数绝对值，"
         "对稠密相似度和 BM25 分数这两种量纲不同的分数天然鲁棒。"
         "RRF 的公式是累加每个文档在各路结果中的排名倒数，"
         "排名越靠前贡献越大，最后按总分排序。"
         "混合检索加上重排序，能兼顾语义匹配和精确匹配，"
         "但代价是多了稀疏索引和融合的开销。"
     )},
]

# 60 条评测 query：gt 为正确答案所在小节 id 列表
QUERIES = [
    # ---- fact 事实抽取（15） ----
    {"q": "B+树和B树相比有什么优势", "gt": ["D01"], "cat": "fact"},
    {"q": "B+树为什么适合范围查询", "gt": ["D01"], "cat": "fact"},
    {"q": "哈希索引为什么不能做范围查询", "gt": ["D02"], "cat": "fact"},
    {"q": "什么是回表操作", "gt": ["D03"], "cat": "fact"},
    {"q": "联合索引的最左前缀原则是什么", "gt": ["D04"], "cat": "fact"},
    {"q": "布隆过滤器是怎么工作的", "gt": ["C01"], "cat": "fact"},
    {"q": "LRU和LFU分别是什么", "gt": ["C04"], "cat": "fact"},
    {"q": "消息队列的削峰填谷是什么意思", "gt": ["M01"], "cat": "fact"},
    {"q": "死信队列是用来干什么的", "gt": ["M03"], "cat": "fact"},
    {"q": "CAP定理说的是什么", "gt": ["T01"], "cat": "fact"},
    {"q": "两阶段提交2PC是怎么工作的", "gt": ["T03"], "cat": "fact"},
    {"q": "一致性哈希为什么能减少数据迁移", "gt": ["L02"], "cat": "fact"},
    {"q": "Redis集群的哈希槽是怎么划分的", "gt": ["L03"], "cat": "fact"},
    {"q": "HNSW的两个参数M和ef分别控制什么", "gt": ["V02"], "cat": "fact"},
    {"q": "RAG的流程分哪几个阶段", "gt": ["G01"], "cat": "fact"},

    # ---- paraphrase 同义改写（15，含易混淆） ----
    {"q": "数据库查询慢，为什么建B+树索引有效", "gt": ["D01"], "cat": "paraphrase"},
    {"q": "怎么让查询不用再回到主表去拿数据", "gt": ["D03"], "cat": "paraphrase"},
    {"q": "有人一直查一个根本不存在的用户，怎么防", "gt": ["C01"], "cat": "paraphrase"},
    {"q": "一个很火的商品缓存突然过期了，怎么防止数据库被打崩", "gt": ["C02"], "cat": "paraphrase"},
    {"q": "一大堆缓存在同一个时间点过期了怎么办", "gt": ["C03"], "cat": "paraphrase"},
    {"q": "缓存空间不够了，应该优先删哪些数据", "gt": ["C04"], "cat": "paraphrase"},
    {"q": "秒杀的时候流量一下很大，后端扛不住怎么办", "gt": ["M01"], "cat": "paraphrase"},
    {"q": "同一条消息被重复消费了怎么处理", "gt": ["M02"], "cat": "paraphrase"},
    {"q": "分布式环境下为什么没办法同时保证一致和可用", "gt": ["T01"], "cat": "paraphrase"},
    {"q": "跨库的操作要保证都成功，有什么办法", "gt": ["T03"], "cat": "paraphrase"},
    {"q": "加机器或者下线机器的时候，缓存怎么尽量少地重分配", "gt": ["L02"], "cat": "paraphrase"},
    {"q": "怎么控制接口每秒只允许那么多请求", "gt": ["R01"], "cat": "paraphrase"},
    {"q": "下游服务挂了，我这边怎么快速失败而不是一直等着", "gt": ["R04"], "cat": "paraphrase"},
    {"q": "两个词长得不一样但意思一样，检索怎么也能找到", "gt": ["V01"], "cat": "paraphrase"},
    {"q": "为什么要先召回一批再做一次精排", "gt": ["G02"], "cat": "paraphrase"},

    # ---- multi 跨块组合（15） ----
    {"q": "消息队列和限流是怎么配合起来做削峰的", "gt": ["M01", "R01"], "cat": "multi"},
    {"q": "缓存失效后流量打过来，除了加锁还能怎么保护数据库", "gt": ["C02", "R04"], "cat": "multi"},
    {"q": "分布式事务为什么往往做不到强一致只能最终一致", "gt": ["T01", "T02"], "cat": "multi"},
    {"q": "向量检索和关键词检索各自的优缺点是什么", "gt": ["V01", "G04"], "cat": "multi"},
    {"q": "RAG为什么要在召回之后再做重排序", "gt": ["G01", "G02"], "cat": "multi"},
    {"q": "为什么RAG能缓解大模型的幻觉问题", "gt": ["G01", "V01"], "cat": "multi"},
    {"q": "一致性哈希和哈希槽都是分片，区别是什么", "gt": ["L02", "L03"], "cat": "multi"},
    {"q": "父子分块是怎么同时解决召回精度和上下文完整性的", "gt": ["G03"], "cat": "multi"},
    {"q": "RRF为什么适合融合稠密和稀疏两路结果", "gt": ["G04"], "cat": "multi"},
    {"q": "布隆过滤器和缓存空值在解决穿透上有什么不同", "gt": ["C01"], "cat": "multi"},
    {"q": "令牌桶和漏桶都是限流，核心区别在哪", "gt": ["R02", "R03"], "cat": "multi"},
    {"q": "覆盖索引和联合索引是什么关系", "gt": ["D03", "D04"], "cat": "multi"},
    {"q": "交叉编码器为什么比双塔更准但又更慢", "gt": ["G02"], "cat": "multi"},
    {"q": "HNSW和IVF相比各有什么优劣", "gt": ["V02", "V03"], "cat": "multi"},
    {"q": "CAP和BASE是什么关系", "gt": ["T01", "T02"], "cat": "multi"},

    # ---- exact 精确术语（15，含易混淆） ----
    {"q": "缓存穿透", "gt": ["C01"], "cat": "exact"},
    {"q": "缓存击穿", "gt": ["C02"], "cat": "exact"},
    {"q": "缓存雪崩", "gt": ["C03"], "cat": "exact"},
    {"q": "令牌桶算法", "gt": ["R02"], "cat": "exact"},
    {"q": "漏桶算法", "gt": ["R03"], "cat": "exact"},
    {"q": "BASE理论", "gt": ["T02"], "cat": "exact"},
    {"q": "TCC事务", "gt": ["T04"], "cat": "exact"},
    {"q": "HNSW算法", "gt": ["V02"], "cat": "exact"},
    {"q": "IVF倒排索引", "gt": ["V03"], "cat": "exact"},
    {"q": "RRF倒数排名融合", "gt": ["G04"], "cat": "exact"},
    {"q": "BM25", "gt": ["G04"], "cat": "exact"},
    {"q": "bge-reranker", "gt": ["G02"], "cat": "exact"},
    {"q": "CAP定理", "gt": ["T01"], "cat": "exact"},
    {"q": "2PC", "gt": ["T03"], "cat": "exact"},
    {"q": "LRU", "gt": ["C04"], "cat": "exact"},
]

def stats():
    from collections import Counter
    c = Counter(q["cat"] for q in QUERIES)
    print("query 总数:", len(QUERIES), dict(c))
    print("语料小节数:", len(SECTIONS))
    print("语料总字数:", sum(len(s["text"]) for s in SECTIONS))

if __name__ == "__main__":
    stats()
